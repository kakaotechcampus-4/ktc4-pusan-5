# 이 파일을 복사해서 ai/.env 를 만든다. .env 는 git 에 올라가지 않는다.
#   cp .env.example .env

# backend 와 같은 Postgres 를 본다. 기본값이 맞으면 건드릴 필요 없다.
# DATABASE_URL=postgresql+asyncpg://basis:basis@localhost:5432/basis

# 외부 호출 타임아웃(초). 네이버 증권 리서치는 인증이 없어서 API 키가 필요 없다.
# HTTP_TIMEOUT_SEC=30
